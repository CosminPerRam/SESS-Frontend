import { Window } from "../Window";

export const AddServer = ({ nodeRef }) => {
  return (
    <Window title={`Add Server - Servers`} nodeRef={nodeRef}>
      <div>Buna siua</div>
    </Window>
  );
};
